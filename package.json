{
  "name": "auth-challenge",
  "version": "1.0.0",
  "description": "I'm challenged to do authentication with firebase, react, node and mongodb for full stack developer position",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run start --prefix client",
    "server": "nodemon index.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "start": "node index.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [
    "firbase authentication",
    "react authentication"
  ],
  "author": "Ezedin Fedlu",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.3"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^1.19.1"
  }
}
